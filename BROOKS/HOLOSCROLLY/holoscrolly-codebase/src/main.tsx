import React from "react";
import ReactDOM from "react-dom/client";
import { HoloscrollyApp } from "./components/HoloscrollyApp";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <HoloscrollyApp />
  </React.StrictMode>
);
